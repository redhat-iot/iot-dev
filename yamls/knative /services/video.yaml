apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: video-service
spec:
  template:
    spec:
      timeoutSeconds: 120
      containers:
        - image: quay.io/astoycos/kafka-knative-source:latest
          ports:
            - containerPort: 8000